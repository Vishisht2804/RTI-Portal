# Loaded by `vite` (dev server). Demo Mode ON so the frontend runs with the
# backend completely stopped. Override for real-backend dev with .env.local
# (VITE_DEMO_MODE=false) or: VITE_DEMO_MODE=false npm run dev
VITE_DEMO_MODE=true
